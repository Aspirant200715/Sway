{
  "name": "social-media-frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix Sway run dev --",
    "build": "npm --prefix Sway run build",
    "lint": "npm --prefix Sway run lint",
    "preview": "npm --prefix Sway run preview"
  }
}
